The trusted UI-only negative control must be generated against the frozen payment portal once its source exists. It must be a real displayed UI change inside the application allowlist and leave POST /api/payments unchanged and exploitable. Save as ui-only.diff. Its absence intentionally blocks the independent gate. No correct repair fixture is supplied: the actual isolated Codex executor must create the candidate.
